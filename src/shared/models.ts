export interface Ballot {
    signature: string;
    id: string;
}

export interface Profile {
    first_name: string;
    last_name: string;
    address: string;
    dob: string;
    driv_lic: string;
}
